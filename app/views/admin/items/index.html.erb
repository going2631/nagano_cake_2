<h1>商品一覧</h1>

<%= @items.each do |item|%>
    <%= item.id%>
    <%= link_to "#{item.name}", admin_items_path(item.id)%>
    <%= item.price%>
    <%= item.genre_id%>
    <%= item.is_active%>

<% end %>